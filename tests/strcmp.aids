enough foreplay

moan name is "yes"
if name as hot as "yes"
    yell "match!"
boundaries respected.

moan a is "apple"
moan b is "banana"
if a colder than b
    yell "apple before banana"
boundaries respected.

if b hotter than a
    yell "banana after apple"
boundaries respected.

moan empty is ""
if empty is frigid
    yell "empty is frigid"
boundaries respected.

I came.
